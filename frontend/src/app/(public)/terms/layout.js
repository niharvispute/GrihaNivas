export const metadata = {
  title: 'Terms of Use',
  description: 'The guiding principles for accessing GrihaNivas — Mumbai\'s premier editorial real estate platform and concierge services.',
};

export default function TermsLayout({ children }) {
  return children;
}
