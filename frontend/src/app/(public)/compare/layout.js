export const metadata = {
  title: 'Compare Properties',
  description: 'Side-by-side comparison of Mumbai properties. Evaluate BHK, area, price, and amenities to make an informed decision.',
};

export default function CompareLayout({ children }) {
  return children;
}
