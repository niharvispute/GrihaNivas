export const metadata = {
  title: 'About Us',
  description:
    'GrihaNivas is a Mumbai-focused real estate advisory — verified listings, trusted builders, and area-led guidance for buyers, renters, and investors.',
};

export default function AboutLayout({ children }) {
  return children;
}
