# Bricks API — Post-Deployment Test Report

> Generated: **2026-04-22 12:06:45**
> App: `http://localhost:5000` (backend) · `http://localhost:3000` (frontend)

---

## Summary

| Metric | Count |
|--------|-------|
| ✅ Passed | **94** |
| ❌ Failed | **67** |
| ⚠️ Warnings | **4** |
| 🚨 Critical Failures | **22** |

## Results by Skill

| Skill | Pass | Fail | Status |
|-------|------|------|--------|
| Functional (01) | 1 | 18 | 🚨 |
| Integration (02) | 7 | 4 | 🚨 |
| API Contract (03) | 7 | 22 | 🚨 |
| Database (04) | 8 | 2 | 🚨 |
| Security (05) | 12 | 9 | 🚨 |
| Performance (06) | 8 | 0 | ✅ |
| Cross-Browser (07) | 12 | 6 | ⚠️ |
| Accessibility (08) | 23 | 1 | ⚠️ |
| Regression (09) | 6 | 4 | 🚨 |
| Smoke (10) | 5 | 0 | ✅ |
| Error Handling (11) | 5 | 5 | 🚨 |

## Failures (sorted by severity)

### ❌ [CRITICAL] GET /api/auth/me without token returns 401
**Skill:** Functional (01)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must return 401 when Authorization header is missing
```

### ❌ [CRITICAL] GET /api/leads (admin-only) without token returns 401
**Skill:** Functional (01)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject unauthenticated requests
```

### ❌ [CRITICAL] GET /api/auth/me returns 401 without token
**Skill:** API Contract (03)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject requests missing Authorization header
```

### ❌ [CRITICAL] GET /api/leads returns 401 without token
**Skill:** API Contract (03)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject requests missing Authorization header
```

### ❌ [CRITICAL] GET /api/users/me returns 401 without token
**Skill:** API Contract (03)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject requests missing Authorization header
```

### ❌ [CRITICAL] GET /api/dashboard returns 401 without token
**Skill:** API Contract (03)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject requests missing Authorization header
```

### ❌ [CRITICAL] GET /api/properties/admin returns 401 without token
**Skill:** API Contract (03)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject requests missing Authorization header
```

### ❌ [CRITICAL] GET /api/leads/export returns 401 without token
**Skill:** API Contract (03)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject requests missing Authorization header
```

### ❌ [CRITICAL] GET /api/users/export returns 401 without token
**Skill:** API Contract (03)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject requests missing Authorization header
```

### ❌ [CRITICAL] GET /api/testimonials/export returns 401 without token
**Skill:** API Contract (03)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject requests missing Authorization header
```

### ❌ [CRITICAL] Auth bypass: GET /api/auth/me without token → 401
**Skill:** Security (05)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must be applied to /api/auth/me
```

### ❌ [CRITICAL] Auth bypass: GET /api/leads without token → 401
**Skill:** Security (05)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must be applied to /api/leads
```

### ❌ [CRITICAL] Auth bypass: GET /api/users/me without token → 401
**Skill:** Security (05)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must be applied to /api/users/me
```

### ❌ [CRITICAL] Auth bypass: GET /api/dashboard without token → 401
**Skill:** Security (05)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must be applied to /api/dashboard
```

### ❌ [CRITICAL] Auth bypass: GET /api/properties/admin without token → 401
**Skill:** Security (05)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must be applied to /api/properties/admin
```

### ❌ [CRITICAL] Auth bypass: POST /api/leads without token → 401
**Skill:** Security (05)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must be applied to /api/leads
```

### ❌ [CRITICAL] Auth bypass: GET /api/leads/export without token → 401
**Skill:** Security (05)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must be applied to /api/leads/export
```

### ❌ [CRITICAL] Fake JWT on admin route is rejected (401/403)
**Skill:** Security (05)

**Expected:** HTTP 401 or 403
**Actual:** HTTP 429

**Fix:**
```
JWT verification must check signature — ensure JWT_SECRET is set and not empty
```

### ❌ [CRITICAL] GET /api/users (admin list) returns 401 without token
**Skill:** Security (05)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
Users list route must be admin-only
```

### ❌ [CRITICAL] R3: GET /api/properties returns data array
**Skill:** Regression (09)

**Expected:** { success: true, data: [] }
**Actual:** HTTP 429, success=false

### ❌ [CRITICAL] R5: GET /api/auth/me without token returns 401
**Skill:** Regression (09)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
protect middleware must reject missing Authorization header
```

### ❌ [CRITICAL] R6: GET /api/leads (admin) without token returns 401
**Skill:** Regression (09)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
adminOnly middleware — verify it is applied to leads routes
```

### ❌ [HIGH] POST /api/calculators/emi returns monthly EMI
**Skill:** Functional (01)

**Expected:** HTTP 200 with { data: { emi: <number> } }
**Actual:** HTTP 429 — emi=undefined

### ❌ [HIGH] POST /api/calculators/stamp-duty returns stamp duty amount
**Skill:** Functional (01)

**Expected:** HTTP 200 with { data: { stampDuty: <number> } }
**Actual:** HTTP 429

### ❌ [HIGH] GET /api/properties returns paginated list
**Skill:** Functional (01)

**Expected:** HTTP 200 with { data: [] }
**Actual:** HTTP 429, data type: undefined

### ❌ [HIGH] GET /api/properties/:id with invalid ID does not crash (returns 400/404)
**Skill:** Functional (01)

**Expected:** HTTP 400 or 404
**Actual:** HTTP 429

**Fix:**
```
Add ObjectId validation to schema validators or use CastError handler in globalErrorHandler
```

### ❌ [HIGH] GET /api/blogs returns list
**Skill:** Functional (01)

**Expected:** HTTP 200 with { data: [] }
**Actual:** HTTP 429

### ❌ [HIGH] POST /api/auth/login with wrong password returns 401
**Skill:** Functional (01)

**Expected:** HTTP 401
**Actual:** HTTP 429

**Fix:**
```
Auth controller must compare password hash and return 401 on mismatch
```

### ❌ [HIGH] GET /api/properties response shape has { success, data[] }
**Skill:** Integration (02)

**Expected:** { success: bool, data: Array }
**Actual:** Keys present: success, message, retryAfter

**Fix:**
```
Ensure propertyController.list uses apiResponse.success() wrapper
```

### ❌ [HIGH] EMI calculator result is mathematically correct (±₹500)
**Skill:** Integration (02)

**Expected:** EMI ≈ ₹87916 (±500)
**Actual:** EMI = ₹undefined

**Fix:**
```
EMI formula: EMI = P * r * (1+r)^n / ((1+r)^n - 1) where r = annualRate/12/100
```

### ❌ [HIGH] GET /api/properties returns 200
**Skill:** API Contract (03)

**Expected:** HTTP 200
**Actual:** HTTP 429

### ❌ [HIGH] GET /api/blogs returns 200
**Skill:** API Contract (03)

**Expected:** HTTP 200
**Actual:** HTTP 429

### ❌ [HIGH] GET /api/testimonials returns 200
**Skill:** API Contract (03)

**Expected:** HTTP 200
**Actual:** HTTP 429

### ❌ [HIGH] GET /api/builders returns 200
**Skill:** API Contract (03)

**Expected:** HTTP 200
**Actual:** HTTP 429

### ❌ [HIGH] GET /api/system/config returns 200/404
**Skill:** API Contract (03)

**Expected:** HTTP 200 or 404
**Actual:** HTTP 429

### ❌ [HIGH] GET /api/banners returns 200/404
**Skill:** API Contract (03)

**Expected:** HTTP 200 or 404
**Actual:** HTTP 429

### ❌ [HIGH] POST /api/calculators/emi with empty body → 400/422 (EMI calculator with empty body)
**Skill:** API Contract (03)

**Expected:** HTTP 400 or 422
**Actual:** HTTP 429

**Fix:**
```
Add Zod/express-validator schema to /api/calculators/emi POST handler
```

### ❌ [HIGH] POST /api/calculators/stamp-duty with empty body → 400/422 (Stamp duty calculator with empty body)
**Skill:** API Contract (03)

**Expected:** HTTP 400 or 422
**Actual:** HTTP 429

**Fix:**
```
Add Zod/express-validator schema to /api/calculators/stamp-duty POST handler
```

### ❌ [HIGH] POST /api/contact with empty body → 400/422 (Contact form with empty body)
**Skill:** API Contract (03)

**Expected:** HTTP 400 or 422
**Actual:** HTTP 429

**Fix:**
```
Add Zod/express-validator schema to /api/contact POST handler
```

### ❌ [HIGH] POST /api/auth/login with empty body → 400/422 (Login with empty body)
**Skill:** API Contract (03)

**Expected:** HTTP 400 or 422
**Actual:** HTTP 429

**Fix:**
```
Add Zod/express-validator schema to /api/auth/login POST handler
```

### ❌ [HIGH] POST /api/auth/signup/request with empty body → 400/422 (Signup request with empty body)
**Skill:** API Contract (03)

**Expected:** HTTP 400 or 422
**Actual:** HTTP 429

**Fix:**
```
Add Zod/express-validator schema to /api/auth/signup/request POST handler
```

### ❌ [HIGH] Invalid ObjectId in URL returns 400/404 (Mongoose CastError handled)
**Skill:** Database (04)

**Expected:** HTTP 400 or 404
**Actual:** HTTP 429

**Fix:**
```
In globalErrorHandler, handle err.name === 'CastError':
if (err.name === 'CastError') return res.status(400).json({ success: false, message: 'Invalid ID format' });
```

### ❌ [HIGH] R4: EMI calculator returns valid EMI
**Skill:** Regression (09)

**Expected:** HTTP 200 with emi > 0
**Actual:** HTTP 429, emi=undefined

### ❌ [HIGH] Malformed JSON body → 400 (not 500 crash)
**Skill:** Error Handling (11)

**Expected:** HTTP 400 — Express body-parser rejects malformed JSON
**Actual:** HTTP 500

**Fix:**
```
Express's built-in JSON parser already returns 400 for malformed JSON — verify globalErrorHandler does not re-throw as 500
```

### ❌ [MEDIUM] POST /api/calculators/emi rejects invalid inputs with 400
**Skill:** Functional (01)

**Expected:** HTTP 400/422
**Actual:** HTTP 429

### ❌ [MEDIUM] GET /api/properties?category=buy&bhk=2 filters correctly
**Skill:** Functional (01)

**Expected:** HTTP 200
**Actual:** HTTP 429

### ❌ [MEDIUM] GET /api/properties/:id with fake ObjectId returns 404
**Skill:** Functional (01)

**Expected:** HTTP 404 with { success: false }
**Actual:** HTTP 429

**Fix:**
```
Controller should check if null result and call next(new AppError(404))
```

### ❌ [MEDIUM] GET /api/blogs/:slug with non-existent slug returns 404
**Skill:** Functional (01)

**Expected:** HTTP 404
**Actual:** HTTP 429

### ❌ [MEDIUM] GET /api/testimonials returns list
**Skill:** Functional (01)

**Expected:** HTTP 200
**Actual:** HTTP 429

### ❌ [MEDIUM] GET /api/builders returns list
**Skill:** Functional (01)

**Expected:** HTTP 200
**Actual:** HTTP 429

### ❌ [MEDIUM] POST /api/contact with empty body returns 400
**Skill:** Functional (01)

**Expected:** HTTP 400/422 validation error
**Actual:** HTTP 429

### ❌ [MEDIUM] POST /api/auth/signup/request with invalid email returns 400
**Skill:** Functional (01)

**Expected:** HTTP 400/422
**Actual:** HTTP 429

### ❌ [MEDIUM] GET /api/properties response includes pagination metadata
**Skill:** Integration (02)

**Expected:** response.pagination or response.meta with total/page/pages
**Actual:** Keys: success, message, retryAfter

### ❌ [MEDIUM] GET /api/nonexistent returns 404 (not 500)
**Skill:** API Contract (03)

**Expected:** HTTP 404 or 400
**Actual:** HTTP 429

### ❌ [MEDIUM] POST with extra unknown fields does not crash (returns 200)
**Skill:** API Contract (03)

**Expected:** HTTP 200 — unknown fields silently stripped
**Actual:** HTTP 429

### ❌ [MEDIUM] Successful response always includes { success: true }
**Skill:** API Contract (03)

**Expected:** { success: true, data: ... }
**Actual:** success = false

**Fix:**
```
Ensure all controllers use apiResponse.success() — see utils/apiResponse.js
```

### ❌ [MEDIUM] EMI calc: string principal rejected with 400/422
**Skill:** Database (04)

**Expected:** HTTP 400 — principal must be a number
**Actual:** HTTP 429

**Fix:**
```
Zod schema: z.number() for principal field in calculator schema
```

### ❌ [MEDIUM] chromium @ 375px: no console errors
**Skill:** Cross-Browser (07)

**Expected:** 0 console errors
**Actual:** 4 error(s): Failed to load resource: the server responded with a status of 429 (Too Many Requests); Failed to load resource: the server responded with a status of 429 (Too Many Requests)

### ❌ [MEDIUM] chromium @ 768px: no console errors
**Skill:** Cross-Browser (07)

**Expected:** 0 console errors
**Actual:** 4 error(s): Failed to load resource: the server responded with a status of 429 (Too Many Requests); Failed to load resource: the server responded with a status of 429 (Too Many Requests)

### ❌ [MEDIUM] chromium @ 1280px: no console errors
**Skill:** Cross-Browser (07)

**Expected:** 0 console errors
**Actual:** 4 error(s): Failed to load resource: the server responded with a status of 429 (Too Many Requests); Failed to load resource: the server responded with a status of 429 (Too Many Requests)

### ❌ [MEDIUM] webkit @ 375px: no console errors
**Skill:** Cross-Browser (07)

**Expected:** 0 console errors
**Actual:** 4 error(s): Failed to load resource: the server responded with a status of 429 (Too Many Requests); Failed to load resource: the server responded with a status of 429 (Too Many Requests)

### ❌ [MEDIUM] webkit @ 768px: no console errors
**Skill:** Cross-Browser (07)

**Expected:** 0 console errors
**Actual:** 4 error(s): Failed to load resource: the server responded with a status of 429 (Too Many Requests); Failed to load resource: the server responded with a status of 429 (Too Many Requests)

### ❌ [MEDIUM] webkit @ 1280px: no console errors
**Skill:** Cross-Browser (07)

**Expected:** 0 console errors
**Actual:** 4 error(s): Failed to load resource: the server responded with a status of 429 (Too Many Requests); Failed to load resource: the server responded with a status of 429 (Too Many Requests)

### ❌ [MEDIUM] Non-existent route returns 404 JSON (not HTML)
**Skill:** Error Handling (11)

**Expected:** HTTP 404 with JSON body { success: false }
**Actual:** HTTP 429, body type: object

**Fix:**
```
notFoundHandler in middleware/errorHandler.js must return JSON:
res.status(404).json({ success: false, message: 'Route not found' })
```

### ❌ [MEDIUM] DELETE /api/properties with fake ID returns 401/404 (not crash)
**Skill:** Error Handling (11)

**Expected:** HTTP 401 (unauth) or 404 (not found)
**Actual:** HTTP 429

### ❌ [MEDIUM] Oversized payload (11kb) returns 413 (limit is 10kb)
**Skill:** Error Handling (11)

**Expected:** HTTP 413 — payload too large
**Actual:** HTTP 500

**Fix:**
```
app.use(express.json({ limit: "10kb" })) in app.js — already configured
```

### ❌ [MEDIUM] Login with empty string credentials returns 400 (not 500)
**Skill:** Error Handling (11)

**Expected:** HTTP 400/401/422
**Actual:** HTTP 429

**Fix:**
```
Zod schema: z.string().min(1) for email and password fields
```

### ❌ [LOW] GET /api/properties with invalid category returns 400
**Skill:** Functional (01)

**Expected:** HTTP 400 or 200 (graceful ignore)
**Actual:** HTTP 429

### ❌ [LOW] GET /api/system/config returns platform config
**Skill:** Functional (01)

**Expected:** HTTP 200
**Actual:** HTTP 429

### ❌ [LOW] GET /api/banners returns array
**Skill:** Integration (02)

**Expected:** HTTP 200 with { data: [] }
**Actual:** HTTP 429

### ❌ [LOW] Blogs: heading hierarchy has no skipped levels
**Skill:** Accessibility (08)

**Expected:** 0 heading level skips
**Actual:** 1 skip(s) detected

**Fix:**
```
Use h1 → h2 → h3 in order, do not jump from h1 to h3
```

## Performance Metrics

### API Response Times

| Endpoint | Avg (ms) | P95 (ms) | P99 (ms) | Flag |
|----------|----------|----------|----------|------|
| `/api/properties` | 1 | 1 | 1 | ✅ |
| `/api/blogs` | 1 | 2 | 2 | ✅ |
| `/api/testimonials` | 1 | 4 | 4 | ✅ |
| `/api/builders` | 1 | 1 | 1 | ✅ |
| `/api/system/config` | 1 | 1 | 1 | ✅ |
| `/api/properties (50x concurrent)` | 17 | 19 | 19 | ✅ |
| `POST /api/calculators/emi (20x concurrent)` | 4 | 6 | 6 | ✅ |

## Optimization Recommendations

### High Impact

#### Mongoose .find() without .select() — fetches all fields
**File:** `/backend/controllers/blogController.js:296`

**Issue:** Fetching all document fields over the wire increases payload size and memory usage.

**Current:**
```js
const comment = blog.comments.find((item) => item._id.toString() === commentId);
```
**Optimized:**
```js
const comment = blog.comments.find((item) => item._id.toString() === commentId);
  .select("title slug price category location.area status createdAt")
```

#### Mongoose .find() without .select() — fetches all fields
**File:** `/backend/controllers/leadController.js:119`

**Issue:** Fetching all document fields over the wire increases payload size and memory usage.

**Current:**
```js
const leads = await Lead.find(filter)
```
**Optimized:**
```js
const leads = await Lead.find(filter)
  .select("title slug price category location.area status createdAt")
```

#### Mongoose .find() without .select() — fetches all fields
**File:** `/backend/controllers/propertyController.js:350`

**Issue:** Fetching all document fields over the wire increases payload size and memory usage.

**Current:**
```js
Property.find(filter)
```
**Optimized:**
```js
Property.find(filter)
  .select("title slug price category location.area status createdAt")
```

#### Mongoose .find() without .select() — fetches all fields
**File:** `/backend/controllers/propertySubmissionController.js:86`

**Issue:** Fetching all document fields over the wire increases payload size and memory usage.

**Current:**
```js
PropertySubmission.find(filter)
```
**Optimized:**
```js
PropertySubmission.find(filter)
  .select("title slug price category location.area status createdAt")
```

#### Mongoose .find() without .select() — fetches all fields
**File:** `/backend/controllers/propertySubmissionController.js:117`

**Issue:** Fetching all document fields over the wire increases payload size and memory usage.

**Current:**
```js
PropertySubmission.find(filter)
```
**Optimized:**
```js
PropertySubmission.find(filter)
  .select("title slug price category location.area status createdAt")
```

#### No response-level caching detected
**File:** `backend/controllers/`

**Issue:** Property listings and public endpoints are hit on every request with no caching layer.

**Current:**
```js
// No cache
const properties = await Property.find(filter).limit(20);
```
**Optimized:**
```js
const cacheKey = 'props:' + JSON.stringify(filter);
const cached = await redis.get(cacheKey);
if (cached) return res.json(JSON.parse(cached));
const properties = await Property.find(filter).limit(20);
await redis.set(cacheKey, JSON.stringify(properties), { EX: 300 }); // 5 min TTL
```

### Medium Impact

#### No code splitting (React.lazy / next/dynamic) detected
**File:** `frontend/src/`

**Issue:** All components are bundled in the initial JS bundle, increasing TTI (Time to Interactive).

**Current:**
```js
import HeavyComponent from './HeavyComponent'
```
**Optimized:**
```js
import dynamic from 'next/dynamic';
const HeavyComponent = dynamic(() => import('./HeavyComponent'), { ssr: false });
```

#### useEffect missing dependency array — causes unnecessary re-renders
**File:** `/frontend/src/components/forms/LeadForm.jsx:34`

**Issue:** useEffect without dependency array runs on every render, causing API calls to repeat unnecessarily.

**Current:**
```js
useEffect(() => { fetchData(); })
```
**Optimized:**
```js
useEffect(() => { fetchData(); }, []) // add dependencies that fetchData uses
```

#### useEffect missing dependency array — causes unnecessary re-renders
**File:** `/frontend/src/components/home/HeroSearch.jsx:28`

**Issue:** useEffect without dependency array runs on every render, causing API calls to repeat unnecessarily.

**Current:**
```js
useEffect(() => { fetchData(); })
```
**Optimized:**
```js
useEffect(() => { fetchData(); }, []) // add dependencies that fetchData uses
```

#### useEffect missing dependency array — causes unnecessary re-renders
**File:** `/frontend/src/components/property/details/PropertyLeadForm.jsx:23`

**Issue:** useEffect without dependency array runs on every render, causing API calls to repeat unnecessarily.

**Current:**
```js
useEffect(() => { fetchData(); })
```
**Optimized:**
```js
useEffect(() => { fetchData(); }, []) // add dependencies that fetchData uses
```

#### Images without explicit width/height cause layout shift (CLS)
**File:** `/frontend/src/app/(public)/about/page.js`

**Issue:** 3 <img> tag(s) without explicit dimensions — browser cannot reserve space before image loads.

**Current:**
```js
<img src={url} alt="Property" />
```
**Optimized:**
```js
<Image src={url} alt="Property" width={400} height={300} /> // next/image
```

#### Images without explicit width/height cause layout shift (CLS)
**File:** `/frontend/src/app/(public)/blogs/page.js`

**Issue:** 2 <img> tag(s) without explicit dimensions — browser cannot reserve space before image loads.

**Current:**
```js
<img src={url} alt="Property" />
```
**Optimized:**
```js
<Image src={url} alt="Property" width={400} height={300} /> // next/image
```

#### Images without explicit width/height cause layout shift (CLS)
**File:** `/frontend/src/app/(public)/contact/page.js`

**Issue:** 2 <img> tag(s) without explicit dimensions — browser cannot reserve space before image loads.

**Current:**
```js
<img src={url} alt="Property" />
```
**Optimized:**
```js
<Image src={url} alt="Property" width={400} height={300} /> // next/image
```

#### Images without explicit width/height cause layout shift (CLS)
**File:** `/frontend/src/app/(public)/home-loan/page.js`

**Issue:** 1 <img> tag(s) without explicit dimensions — browser cannot reserve space before image loads.

**Current:**
```js
<img src={url} alt="Property" />
```
**Optimized:**
```js
<Image src={url} alt="Property" width={400} height={300} /> // next/image
```

#### Images without explicit width/height cause layout shift (CLS)
**File:** `/frontend/src/app/(public)/page.js`

**Issue:** 4 <img> tag(s) without explicit dimensions — browser cannot reserve space before image loads.

**Current:**
```js
<img src={url} alt="Property" />
```
**Optimized:**
```js
<Image src={url} alt="Property" width={400} height={300} /> // next/image
```

#### Images without explicit width/height cause layout shift (CLS)
**File:** `/frontend/src/app/(public)/rent-agreement/page.js`

**Issue:** 2 <img> tag(s) without explicit dimensions — browser cannot reserve space before image loads.

**Current:**
```js
<img src={url} alt="Property" />
```
**Optimized:**
```js
<Image src={url} alt="Property" width={400} height={300} /> // next/image
```

#### Images without explicit width/height cause layout shift (CLS)
**File:** `/frontend/src/app/admin/builders/page.js`

**Issue:** 1 <img> tag(s) without explicit dimensions — browser cannot reserve space before image loads.

**Current:**
```js
<img src={url} alt="Property" />
```
**Optimized:**
```js
<Image src={url} alt="Property" width={400} height={300} /> // next/image
```

#### Images without explicit width/height cause layout shift (CLS)
**File:** `/frontend/src/app/admin/properties/page.js`

**Issue:** 1 <img> tag(s) without explicit dimensions — browser cannot reserve space before image loads.

**Current:**
```js
<img src={url} alt="Property" />
```
**Optimized:**
```js
<Image src={url} alt="Property" width={400} height={300} /> // next/image
```

#### Images without explicit width/height cause layout shift (CLS)
**File:** `/frontend/src/app/admin/property-submissions/page.js`

**Issue:** 2 <img> tag(s) without explicit dimensions — browser cannot reserve space before image loads.

**Current:**
```js
<img src={url} alt="Property" />
```
**Optimized:**
```js
<Image src={url} alt="Property" width={400} height={300} /> // next/image
```

### Low Impact

#### ✅ Compound indexes defined on Property model
**File:** `backend/models/mongoose/Property.js`

**Issue:** Compound indexes are present — queries on { category, isActive, price } will be fast.

## Quick-Win Checklist

- [ ] Fix all CRITICAL failures first
- [ ] Fix all HIGH severity failures
- [ ] Review MEDIUM severity warnings
- [ ] Apply High-impact optimizations
- [ ] Re-run `node run-all.js --phase smoke` after each fix
- [ ] Re-run `node run-all.js --phase all` before next release
